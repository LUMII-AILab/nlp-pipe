{
  "name": "ui",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "5.0.1"
  },
  "dependencies": {
    "antd": "^4.16.13",
    "debug": "^4.3.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "styled-components": "^5.3.3"
  },
  "scripts": {
    "start": "REACT_APP_API_URL=http://nlp.ailab.lv react-scripts start",
    "start-local": "REACT_APP_API_URL=http://localhost:9100 react-scripts start",
    "build": "react-scripts build",
    "build-static": "react-scripts build && rm -rf ../src/static && mkdir ../src/static && cp -r build/* ../src/static/"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
